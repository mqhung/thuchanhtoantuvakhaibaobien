let inputwidth;
let inputlength;
inputwidth = prompt("Input the width");
inputlength= prompt("Input the length");
let width = parseInt(inputwidth);
let length = parseInt(inputlength);
let area = width * length;
document.write("The area is: "+area);
